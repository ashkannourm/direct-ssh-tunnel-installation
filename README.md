# 🚀 نصب تانل SSH-direct بین سرور ایران و خارج

این پروژه یک پنل گرافیکی تحت وب (GUI) است که با پایتون و **Streamlit** طراحی شده تا فرآیند پیچیده تانل‌زدن بین سرورهای ایران و خارج را کاملاً اتوماتیک کند. 



## ✨ قابلیت‌ها
* **نصب آسان:** راه‌اندازی کل سیستم با یک دستور (One-Line Install).
* **رابط کاربری فارسی:** استفاده از فونت **IRANSansX** و محیط کاملاً راست‌چین (RTL).
* **مدیریت هوشمند کلیدها:** ساخت `ssh-keygen` و انتقال آن با `ssh-copy-id` به صورت خودکار.
* **پایداری بالا:** ساخت خودکار سرویس‌های `systemd` برای اتصال مجدد تانل در صورت قطع شدن سرور.
* **امنیت:** عدم ذخیره دائمی رمز عبور و استفاده از کلید خصوصی برای ارتباطات.

---

## 🛠 پیش‌نیازها
* یک سرور لینوکس (Ubuntu 20.04+) به عنوان سرور مبدا (ایران).
* دسترسی Root در هر دو سرور مبدا و مقصد.
* باز بودن پورت‌های مورد نظر در فایروال هر دو سرور.

---

## 📥 نحوه نصب و اجرا
تنها کافیست دستور زیر را در ترمینال سرور ایران خود کپی و اجرا کنید:

```bash
bash <(curl -Ls https://raw.githubusercontent.com/ashkannourm/direct-ssh-tunnel-installation/main/install.sh)
```

## 📖 نحوه استفاده
پس از اجرای اسکریپت نصب، پنل روی پورت 2552 فعال می‌شود.

آدرس http://IP_SERVER_IRAN:2552 را در مرورگر باز کنید.

اطلاعات سرور مقصد را وارد کنید:

IP سرور مقصد: آدرس سرور خارج.

رمز عبور: رمز Root سرور خارج (فقط برای یک‌بار جهت انتقال کلید).

پورت تانل: پورتی که می‌خواهید روی ایران باز شود (مثلاً 29283).

روی دکمه "نصب و راه‌اندازی تانل" کلیک کنید.

## 📂 ساختار فایل‌های پروژه
app.py: کد اصلی برنامه و رابط کاربری.

install.sh: اسکریپت نصب خودکار و پیکربندی سیستم.

IRANSansX-Medium.woff: فایل فونت فارسی برنامه.

## ⚠️ نکات امنیتی
این برنامه از کلید SSH برای پایداری استفاده می‌کند، بنابراین پس از اولین اتصال، دیگر نیازی به رمز عبور نخواهد بود.

پیشنهاد می‌شود پس از اتمام کار، سرویس پنل را برای امنیت بیشتر متوقف کنید:

```bash
sudo systemctl stop ssh-panel
```
